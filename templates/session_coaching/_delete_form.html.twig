<form method="post" action="{{ path('app_session_coaching_delete', {'id': session_coaching.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ session_coaching.id) }}">
    <br>
    <button class="btn btn-danger">Delete</button>
</form>
