<form method="post" action="{{ path('app_session_boosting_delete', {'id': session_boosting.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ session_boosting.id) }}">
    <br>
    <button class="btn btn-danger">Delete</button>
</form>
