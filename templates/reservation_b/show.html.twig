<!DOCTYPE html>

<title>ReservationB</title>

{% block body %}
    <h1>ReservationB</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ reservation_b.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_reservation_b_index') }}">back to list</a>

    <a href="{{ path('app_reservation_b_edit', {'id': reservation_b.id}) }}">edit</a>

    {{ include('reservation_b/_delete_form.html.twig') }}
{% endblock %}
