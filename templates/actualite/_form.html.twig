{{ form_start(form) }}

<div class="col-sm-12 col-xl-6">
    <div class="bg-secondary rounded h-100 p-4">
        <h6 class="mb-4">Floating Label</h6>
        <div class="form-floating mb-3">
            {{ form_widget(form.Titre, {'attr': {'class': 'form-control', 'placeholder': 'exp: Breaking news'}}) }}
            {{ form_label(form.Titre, 'News Title') }}
        </div>
        
        <div class="form-floating">
            {{ form_widget(form.Description, {'attr': {'class': 'form-control', 'placeholder': 'News description here', 'style': 'height: 150px;'}}) }}
            {{ form_label(form.Description, 'News Description') }}
        </div>

        <div class="bg-secondary rounded h-100 p-4">
                                        
            {{ form_widget(form.Save, {'attr': {'class': 'btn btn-outline-light m-2', 'label':'Save'}}) }}
                                       
         </div>
    </div>
</div>

{{ form_end(form) }}

