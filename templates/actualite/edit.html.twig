{% extends '/backOffice.html.twig' %}
{% block title %}Edit Actualite{% endblock %}

{% block body %}
<div class="container-fluid pt-4 px-20">
        <div class="row g-4">
            <div class="col-sm-20 col-xl-20">
                <div class="bg-secondary rounded h-100 p-4"
    {{ include('actualite/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_actualite_new') }}">back to list</a>

    {{ include('actualite/_delete_form.html.twig') }}
    </div>    </div>
    </div>
    </div>

{% endblock %}
